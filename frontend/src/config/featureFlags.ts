/**
 * Shared UI feature flags.
 *
 * IN_APP_CAMERA_ENABLED: when false, the bottom-nav Camera tab and
 * Post composer camera button are hidden. CameraCapture.tsx is kept
 * in the codebase so this can be turned back on later.
 */
export const IN_APP_CAMERA_ENABLED = false;
